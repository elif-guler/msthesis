#+begin_src bash :tangle markers.sh
  usage="$(basename $0) [-h] -d <downloadDir>
  Use Fur to extract markers from targets and neighbors.
  
  Example: bash markers.sh ecl"
#+end_src


#+begin_src bash :tangle markers.sh
  while getopts "hd:" arg; do
      case $arg in
  	h) echo "$usage"
  	   exit;;
  	d) dir=$OPTARG;;
  	\?) exit 1;;
      esac
  done
#+end_src

#+begin_src bash :tangle markers.sh
  if [[ ! $dir ]]; then
      echo "Please provide a download directory."
      echo "$usage"
      exit 1
  else
      if [[ ! -d $dir ]]; then
  	echo "Download directory $dir doesn't exist."
      fi
  fi
#+end_src


fur'u calistirmak icin download directoryye giriyoruz
targetlar ve neighborlari alip all adinda bir dosya olusturuyoruz

#+begin_src bash :tangle markers.sh
  cd $dir
  makeFurDb -t targets -n neighbors -d all.db
  fur -d all.db |
      cleanSeq > markers.fasta
#+end_src
